{
  "name": "@jacbot/memory",
  "version": "0.1.0",
  "description": "Memory layer for Jacbot — short/medium/long-term agent memory with local vector storage.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@jacbot/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
